{
  config = {
    a = {
      children = [ ];

      attributes = {
        download.enable = true;
        download.filename = "new-filename";

        href.enable = true;
        href.url = "#top";
      };
    };
  };
}
